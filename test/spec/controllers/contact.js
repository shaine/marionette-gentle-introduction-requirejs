(function() {
    'use strict';

    var root = this;

    root.define([
            'controllers/contact'
        ],
        function( contactController ) {

            describe('ContactController Controller', function () {
            });

        });

}).call( this );
