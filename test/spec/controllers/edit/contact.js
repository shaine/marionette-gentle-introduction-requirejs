(function() {
    'use strict';

    var root = this;

    root.define([
            'controllers/edit/contact'
        ],
        function( editContact ) {
            describe('EditContact Controller', function () {
            });
        });

}).call( this );
