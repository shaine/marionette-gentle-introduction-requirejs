define(function() {
    'use strict';

    /* return an array of specs to be run */
    return {
        specs: [
            'spec/collections/contact-collection.js',
            'spec/models/contact.js',
            'spec/views/home.js'
        ]
    };
});
